@model IEnumerable<CSVReader.Models.RegisterAgentsVM>

@{
    ViewBag.Title = "About";
}
<h2>@ViewBag.Title.</h2>
<h3>@ViewBag.Message</h3>

<table>
    <tr>
        <th>Count = </th>
        <th>@Model.Count()</th>
    </tr>

    @*@foreach (var usr in Model)
    {
        <tr>
            <td>@usr.FirstName</td>
            <td>@usr.LastName</td>
        </tr>
    }*@
</table>